clear all;close all
disp('①配列[0,0.1,0.2・・・・・9.9,10]')
x=[0:0.1:10];
disp('②y=sin(x)の作成')
y=sin(x);
disp('③図y=sin(x)')
plot(y)
disp('④ラベル追加')
xlabel('時間[秒]')
ylabel('電圧[V]')
disp('⑤グリッド作成')
grid on
grid minor

disp('考察')

disp('グラフより、この正弦波の周期は”64[s]”と読み取れる')


% ①0から0.1刻みで10までの値を代入
% ②y=sin(x)を作成
% ③xyグラフの作成
% ④軸のラベル表記
% ⑤タイトル表記
% ⑥方眼を付ける
%　周期を求める
